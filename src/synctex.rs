pub struct SyncTex;

impl SyncTex {
    #[allow(dead_code)]
    pub fn pdf_to_editor(_x: f32, _y: f32, _page: u16) -> usize {
        // Placeholder bridge
        0
    }

    #[allow(dead_code)]
    pub fn editor_to_pdf(_offset: usize) -> (f32, f32, u16) {
        // Placeholder bridge
        (0.0, 0.0, 0)
    }
}
