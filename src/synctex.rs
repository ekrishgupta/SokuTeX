pub struct SyncTex;

impl SyncTex {
    pub fn pdf_to_editor(x: f32, y: f32, page: u16) -> usize {
        // Placeholder bridge
        0
    }

    pub fn editor_to_pdf(offset: usize) -> (f32, f32, u16) {
        // Placeholder bridge
        (0.0, 0.0, 0)
    }
}
